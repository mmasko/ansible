---
- hosts: localhost
  vars:
    source_path: /opt/ansible/roles/{{ game }}
    install_path: "/opt/{{ game }}"
    service_state: started
    s3_bucket: gamesmasko
    api_path: https://47ccw0fer8.execute-api.us-west-1.amazonaws.com/production/jakecraft

  roles:
    - role: "/opt/ansible/roles/prepserver"
      vars:
        port: 5000
      tags: prep
    
    - role: "/opt/ansible/roles/{{ game }}"
      vars:
        port: 5000
      tags: install